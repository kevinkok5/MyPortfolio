import Heading from "@/components/Heading";
import React from "react";

const Footer = () => {
    return (
        <div>
            <Heading header="Let's" subheader="Talk" />
        </div>
    );
};

export default Footer;
